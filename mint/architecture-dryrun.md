# Architecture — `apply --dry-run`: output contract + offline/live test boundary (§ dry-run)

> mint-hld round, 2026-06-06. Brief: `mint/design-brief.md` (archived: `design-brief-mirror-redirect.md`).
> Roster: parallel **image-render · image-format · test-boundary** + sequential **contrarian**.
> coherence-reviewer **pass** (round 2) · grounder **clean-with-gates** (0 refuted · 1 spike · 3 needs-grep · 4 disguised-PO reclassified).
> Roadmap item ① (PO Dmitry, locked 2026-06-05). This is the "missing RuntimeImage consumer" the loader-datamodel HLD parked.

## PO rulings (Dmitry, 2026-06-06) — the discharged plan

The synthesis recommends **Option 1 (Recording-fake floor)** as slice 1; the 2 surviving PO-plate forks
are ruled here (both had a defensible engineering answer — decided, not punted):

- **① Slice-1 representation → GOLDEN-ONLY, no CLI verb.** The load-bearing prize per the brief is the
  test boundary (config→map-image offline + CI-green). The literal `apply --dry-run` CLI verb + the
  human-decoded operator view share the *same underlying image* → a zero-rework follow-on (slice 1b).
  Do not gold-plate slice 1 with two representations.
- **② Live-gate shrink → YES, but as a separate bounded slice 2 (Option 4 slice-2).** Roadmap-①'s own
  definition includes "dry-run-DRIVEN migration → thin the redundant heavy ctests". Sequenced AFTER the
  golden format freezes; migrate only the image-side of *genuinely-split* ctests, never touch
  irreducibly-live verdict coverage.

**Plan: Option 1 (slice 1, golden-only) → Option 4 slice-2 (slice 2, bounded gate-shrink), SPIKE-1 first.**

- **SPIKE-1 (THE gate, run throwaway-first like the loader-datamodel slice-2 spike):** prove a `dryrun_harness`
  TU using a fake `xdpfilter_bpf*` (+ fake `bpf_map__fd`/`bpf_map_*_elem`/`resolve_ifindex`) **links with NO
  real libbpf**. PASS → Option 1 as-is. FAIL → degrade to Option 3 factored `render(cr)→MapImage` (sidesteps
  the skel via `CompiledRuleset`, costs a live write-path rewrite) OR a hand-rolled minimal fake-skel struct
  that does NOT `#include build/xdpfilter.skel.h`. A FAIL re-scopes slice 1; it does not kill the round.
- **4 reclassified-off-PO (resolved as engineering, surface as needs-grep in the briefer):** CLI-verb-in-scope
  (→ in-process harness needs no verb), full-apply-write-set vs materialize-body-only (→ action_table/devmap
  at apply sites 2146/2154/2266/2273, outside materialize → golden MUST include them), `resolve_ifindex`
  stub-vs-thread (→ "zero kernel calls" + frozen prod signatures → harness-side stub + symbolic ifindex),
  typed MapImage API (→ YAGNI, no consumer, link-seam gives SSoT without the rewrite).

---

# Synthesis (round 2)

## Convergence (where architects agree)

- **(image-render, image-format, test-boundary, contrarian): the seam already exists and is uniform — the render helpers (`populate_*`, `write_*`) already take a raw `int fd`; the ONLY skeleton weld is inside `materialize()`'s body (~12 `skel->maps.*` dereferences in one function).** Offline render is therefore cheap: no helper-signature churn, only feeding `materialize` a fake skel. Verified: `materialize` (loader.cpp:1637-1713) calls `inactive_axis_fd(skel->maps.X_a, skel->maps.X_b, …)` per axis + two direct `bpf_map__fd(skel->maps.ruleset_state/slot_rule_id)`. [grounded]
- **(image-render, image-format, contrarian): the LPM `mask` is the post-closure output of `close_fn(prefixes)` computed INSIDE `populate_bitvec_inner_slot` (loader.cpp:1266) and is absent from `CompiledRuleset`.** This kills the "print `CompiledRuleset`" slice (image-format SELECT C) on the record — a `cr`-print asserts a pre-closure fiction that passes green through a closure regression. The right oracle is the post-materialize `(map,key,value)` write-set. [needs-grep]
- **(image-render, image-format, test-boundary, contrarian): `resolve_ifindex` (loader.cpp:1583, inside `populate_redirect_devmap`) is the single map cell that is a function of KERNEL state, not config**, and must be rendered symbolically offline (`ifindex=<dpi0> RESOLVED-AT-APPLY`), with the concrete `if_nametoindex` equality staying live. All four converge that stub-resolver-in-harness is the minimal honest seam. [needs-grep]
- **(image-render, test-boundary, contrarian): single-source-of-truth is non-negotiable (guard #9) — a parallel image-builder asserts a fiction the moment it drifts and is worse than no dry-run.** test-boundary explicitly VETOes the parallel-builder from the test seat; the link-seam achieves SSoT "by construction" (identical `materialize` object code). [needs-grep]
- **(image-render, image-format, test-boundary, contrarian): the offline harness follows the established `compile_harness`/`ruleset_delta_harness` mold — bare-main, no gtest, libbpf-free link IS the contract, independent oracle + mandatory NEGATION + SMOKE — wired with NO `require_passwordless_sudo` / NO `XDPMF_CI_BUILD_ONLY` skip / NO `RESOURCE_LOCK`, advancing the B39 CI boundary one stage toward the kernel.** [needs-grep]
- **(image-render, image-format, test-boundary, contrarian): the right image slice is the FULL ordered `(map,key,value)` write-set, occupied-writes-only (bulk-clear delete traffic excluded as kernel-state-dependent), NOT a per-rule summary and NOT the pre-materialize `CompiledRuleset`.** [needs-grep]

## Divergence (where architects substantively disagree)

- **(image-render vs contrarian, on the render mechanism): image-render presents (a-link link-seam) and (b factored `render()→MapImage`) as co-equal selected corners, recommending ① first / ② as a "possible follow-on if the format lens demands a typed API"; contrarian argues ② is "a solution to a problem slice 1 doesn't have" and should be deferred ENTIRELY**, because (b) rewrites the live write path into an image-iterator and re-exposes PI-mvp-4.35-VERDICT-IDENTITY / atomic-swap ordering, while ①'s SSoT is already structural. Implication: whether the typed `MapImage` API is a near-term option or strictly out-of-scope. [needs-grep]
- **(image-format vs contrarian, on output representation): image-format SELECT A recommends "both — human default + `--format=golden`"; contrarian (Hole #2) argues "both" is gold-plating for slice 1, that only the test-oracle audience is load-bearing now, and the human decoder is a separable follow-on.** image-format itself concedes this in its SELECT B ("operator-UX is a co-benefit, not the requirement"), so its own fallback collapses toward the contrarian's position. Implication: the human-vs-golden-vs-both PO fork. [genuine-PO-value: GGSN-Gi NOC operator preview coherence vs test-oracle-first scoping]
- **(test-boundary vs contrarian, on test migration): test-boundary recommends "two slices, A (additive harness) then B (SPLIT + thin the live `.sh` cohort)"; contrarian (Hole #4) argues B is not even slice 2 of THIS feature — it's an independent tidy that can happen "after the golden format freezes, or never,"** because its payoff is gate-shrink not new coverage, and co-shipping risks conflating "new offline proof" with accidentally deleting live verdict coverage. Implication: whether live-ctest surgery is part of the dry-run roadmap item at all. [genuine-PO-value: B39-extension gate-shrink goal vs new-coverage-only]
- **(test-boundary internal / contrarian, on CLI verb placement): test-boundary enumerates in-process (#3) vs CLI-spawned (#4) and recommends in-process/libbpf-free, yet bundles "the dry-run CLI verb + dryrun_harness" into Slice 1; contrarian (Hole #1) flags this as the biggest lever — if the harness drives render in-process, the `apply --dry-run` CLI verb is NOT on the critical path for the test win.** Implication: the named roadmap item ("`apply --dry-run`") may be a thin operator-UX follow-on, while the actual prize (offline test) needs no CLI verb. [needs-grep]

## Composite directions (cross-lens combinations)

### Option 1 — Recording-fake floor (the lean convergence point)
- **Composition:** image-render ①(a-link link-seam recording fake) + ③(fake skel + fake `bpf_map__fd`) + image-format SELECT B (golden-only canonical byte-faithful body) + test-boundary SELECTION A (additive harness, zero ctest migration) + contrarian's integrated lean (defer CLI verb, human view, typed render, test-thinning).
- **First slice scope:** [needs-grep on the move; the harness/format are design]
  1. Pure-move extraction of `materialize` + the render helpers into a libbpf-free `materialize.cpp` — the slice's ONLY prod-side edit; host-only, no `.bpf.c`. [needs-grep]
  2. **WRITE-SET BOUNDARY (resolves the reviewer's UNADDRESSED-HOOK): the offline image is the FULL APPLY write-set, not `materialize()`'s body alone.** Verified: `materialize` (1637-1713) issues the 9 axes + `ruleset_state` + `rules` + `slot_rule_id`, but does NOT issue `action_table` or `redirect_devmap` — those are at the apply call sites (`populate_action_table` loader.cpp:2146/2266, `populate_redirect_devmap` 2154/2273). image-format's recommended golden explicitly contains `action_table` and `redirect_devmap` rows, so a recording-fake driving `materialize()` alone would capture neither map and could not produce the recommended golden. Therefore the harness must drive a thin **apply-write-set sequence** = `materialize(skel, slot, cr)` THEN `populate_action_table(at_fd)` THEN `populate_redirect_devmap(dm_fd, config)` — the same three calls, in the same order, the live apply sites issue. The pure-move must therefore also bring `populate_action_table` + `populate_redirect_devmap` into `materialize.cpp` (they are already `int fd`-parameterized, so this is the same mechanical move). `copy_rule_counters_forward` is NOT in the image (it READS the live old-active inner — irreducibly live; see HA #6). [grounded for the call-site facts; needs-grep for the extraction]
  3. New `tests/dryrun/dryrun_harness.cpp` linking {`materialize.cpp`, `compiled_ruleset.cpp`, fake-bpf-sink TU, fake-skel + fake `bpf_map__fd`, fd-tag→{map,key_sz,val_sz} table, stub `resolve_ifindex`}. The stub resolver already named covers the devmap VALUE (symbolic ifindex). [needs-grep]
  4. ONE canonical golden format (`# xdpfilter-image v1` header, fixed apply-write-set map-order, within-map sorted by stored-key bytes, fixed-width hex of stored bytes, post-closure masks, symbolic ifindex); `T_DRYRUN_IMAGE_IDENTITY` with SMOKE + NEGATION. No CLI verb.
- **Risk profile:** low — the prod path's write logic is untouched; the only prod edit is a mechanical move mirroring B40/B34b; insn 3477, PASS/DROP byte-identity, PI-mvp-4.35-VERDICT-IDENTITY all structurally untouched (host-side). The one new wrinkle the reviewer surfaced — the harness must replicate the apply-site call SEQUENCE (3 calls, fixed order), not just call `materialize` — is a test-side concern, not a prod risk. [needs-grep]
- **User value cycle 1:** the config→map-image correctness class (incl. the post-closure LPM lowering compile_harness stops short of, AND the `action_table`/`redirect_devmap` writes only the live datapath observes today) moves offline + CI-green; one new check, B39 boundary advanced.
- **Costs:** TTFW ~1 cycle [needs-grep]; LOC: +1 fake-bpf TU, +1 fake-skel TU, +1 fd-tag table, +1 harness, +1 golden fixture, ~0 net prod (move) [needs-grep]; deps: none new (libbpf-free). Sacrifices: no `apply --dry-run` verb this cycle; no human-decoded view; no live-gate shrink. [needs-grep]
- **Preserves:** insn baseline 3477, PASS/DROP byte-identity, PI-mvp-4.35-VERDICT-IDENTITY, double-buffer atomic swap, PI-7 loader.hpp, CompiledRuleset/RulesetDelta shapes, B42 redirect verb. Zero kernel calls, zero map writes.
- **Open Qs:** does the fake-skel approach (③) link cleanly against the real `xdpfilter_bpf` skeleton struct type without dragging the skel/libbpf object? [needs-spike]

### Option 2 — Operator-facing dry-run (verb + human view co-shipped)
- **Composition:** Option 1's mechanism + image-format SELECT A (both: human default + `--format=golden`) + the `apply --dry-run` CLI verb threaded into `parse_apply` (**src/cli/cli.cpp:226** — corrects the prior :369 mis-citation; 369/370 is only the dispatch `return parse_apply(rest)`) + test-boundary SELECTION A.
- **First slice scope:** everything in Option 1 PLUS CLI flag threading (`bool dry_run` through `ApplyRequest`), the human pretty-print decoder (byte→typed-field, operator vocabulary from CONFIG_SCHEMA.md, symbolic ifindex names), and `--format=golden` switching. [needs-grep]
- **Risk profile:** med — adds the human decoder (a second representation with its own drift surface) and CLI plumbing; the verb spawns the libbpf-linked binary so an end-to-end CLI test (if added) is offline-run but NOT libbpf-free-linked, weakening the clean-link canary. [needs-grep]
- **User value cycle 1:** delivers the literally-named roadmap item — an operator runs `apply --dry-run` and reads a decoded, vocabulary-correct preview — AND the offline test win.
- **Costs:** TTFW ~1.5–2 cycles [needs-grep]; LOC: Option 1 + human decoder + CLI thread (moderate) [needs-grep]; deps: none new. Sacrifices: more surface to stabilize; two representations to version; the human prose becomes a quasi-API if any test reads it. [needs-grep]
- **Preserves:** same invariants as Option 1 (the verb is read-only/offline by contract).
- **Open Qs:** is the operator preview valuable enough THIS cycle to justify the human-decoder surface, or does deferring it lose nothing (both share the underlying image)? [genuine-PO-value: operator preview now vs test-first]

### Option 3 — Typed MapImage API (the principled-refactor corner)
- **Composition:** image-render ②(factored `render(CompiledRuleset, slot, ifindex) -> MapImage` pure function) + image-format SELECT A + test-boundary SELECTION A. The live `materialize` becomes an image-iterator over a shared typed `MapImage`.
- **First slice scope:** factor the value-lowering (clear-set, prefix-closure, struct packing) out of `materialize` into pure `render()` returning typed fd-free `MapImage`; rewrite the live write path to iterate it; golden + dry-run consume the same structure. **Note: a `render()` returning a `MapImage` is the natural home for the action_table/devmap-outside-materialize problem — render can simply include those writes in the image, so the apply-site call-sequence wrinkle of Option 1 disappears (at the cost of the live-path rewrite).** [needs-grep]
- **Risk profile:** high — rewrites the live write path; byte-identity + write-ORDER of the live writes must be re-proven against HEAD, re-exposing PI-mvp-4.35-VERDICT-IDENTITY and the atomic-swap ordering; needs the full live datapath gate to re-pass. [needs-grep]
- **User value cycle 1:** same offline test win as Option 1, but via a first-class typed `MapImage` API — cleaner, fd-free-by-construction oracle, cleanly parameterizing slot/ifindex (no resolver stub needed — caller passes index).
- **Costs:** TTFW ~2–3 cycles (refactor + re-prove gate) [needs-grep]; LOC: high (value-compute lifted, live loop rewritten) [needs-grep]. Sacrifices: spends verdict-identity risk budget for an API niceness Option 1's link-seam delivers (as SSoT) without the refactor.
- **Preserves:** intends to preserve all invariants but must EARN byte-identity by re-running the live gate (not structural as in Option 1).
- **Open Qs:** does any consumer actually need the typed `MapImage` API, or is it speculative generality? Contrarian argues no consumer exists. [needs-grep / genuine-PO-value]

### Option 4 — Floor + bounded gate-shrink (additive then subtractive)
- **Composition:** Option 1 (slice 1) THEN test-boundary SELECTION B + SELECTION C (per-test SPLIT + thin live ctests, with the hybrid `resolve_ifindex` shape-offline/value-live split for `T_REDIRECT_COUNTER_AND_MAP`).
- **First slice scope:** Option 1's harness ships first; a SECOND slice extracts the image-side `bpftool map dump` assertions from `T_APPLY_VALID_CONFIG`, `T_APPLY_REPLACES_RULESET`, `T_REDIRECT_COUNTER_AND_MAP` into the offline golden and thins those live ctests to verdict-only. [needs-grep]
- **Risk profile:** med — slice 1 is low-risk (Option 1); slice 2 is live-`.sh` surgery whose hazard is accidentally deleting live VERDICT coverage while removing the image half. Bounded by "migrate only the image-side of a genuinely-SPLIT test, never touch irreducibly-live rows." [needs-grep]
- **User value cycle 1:** Option 1's value; cycle 2 additionally SHRINKS the live root-only gate (the B39-extension payoff) and makes those assertions CI-green.
- **Costs:** TTFW ~1 cycle (slice 1) + ~1 cycle (slice 2) [needs-grep]; LOC: Option 1 + deletions in 3 `.sh` files [needs-grep]. Sacrifices: requires careful per-test split review; defers nothing the PO wants if gate-shrink is a goal. [needs-grep]
- **Preserves:** all Option 1 invariants; explicitly preserves ALL live verdict coverage and the irreducibly-live oracle-agreement family. [needs-grep]
- **Open Qs:** which ctests actually split image-vs-verdict cleanly — architects assert `T_APPLY_VALID_CONFIG`/`REPLACES_RULESET`/`REDIRECT_COUNTER_AND_MAP` do, but this needs per-file confirmation. [needs-grep]

## Recommendation (with caveat)

Lean to **Option 1 (Recording-fake floor)** as slice 1, with **Option 4's slice 2 (bounded gate-shrink) as the natural follow-on IF the PO wants the live gate to actually shrink**. [genuine-PO-value: B39-extension gate-shrink vs new-coverage-only] This is where all four lenses converge when pushed to their leanest corner: it delivers the stated value (config→map-image offline + CI-green, incl. the post-closure lowering and the `action_table`/`redirect_devmap` writes no existing offline harness covers) at the lowest risk (the prod write logic is structurally untouched — the only prod edit is a mechanical extraction mirroring B40/B34b), and it cleanly defers every separable concern (CLI verb, human view, typed `MapImage`, test-thinning) without rework, because they all share the one underlying image. [needs-grep]

**The single biggest caveat:** Option 1's runnability hinges on the fake-skel mechanism (③) — feeding `materialize` an `xdpfilter_bpf*` whose `bpf_map__fd` returns tag-fds, linked libbpf-free, AND the harness replicating the apply-site call SEQUENCE (`materialize` → `populate_action_table` → `populate_redirect_devmap`, fixed order) so the golden contains every map the recommended format prints. If the generated skel header transitively forces libbpf symbols that can't be faked at link, the "libbpf-free, zero-prod-logic-change" promise degrades and bends toward Option 3's factored `render()` (which sidesteps the skel by taking `CompiledRuleset` directly AND folds action_table/devmap into the image natively, at the cost of the live-path rewrite). [needs-spike]

## Open questions (need human input)

- **Human-vs-golden-vs-both for slice 1:** ship golden-only (recommended — test-oracle-first, contrarian Hole #2) or co-ship the human-decoded operator view (image-format SELECT A, the literal roadmap-item UX)? [genuine-PO-value: GGSN-Gi NOC operator preview coherence vs lean test-first scoping]
- **Is the `apply --dry-run` CLI verb itself in scope this round, or is the offline test the real deliverable with the verb a thin follow-on?** The named roadmap item is the verb; the prize per the brief is the test boundary. [genuine-PO-value: deliver the literally-named operator feature vs the underlying test-migration value]
- **Does the offline image cover the full apply write-set (incl. `action_table` + `redirect_devmap`) or `materialize()`'s body only?** The recommended golden assumes the FULL set (verified those two writes live at the apply call sites, not in `materialize`). Confirming "full apply write-set" is the right oracle scope is a small design ratification, not a pure code fact. [genuine-PO-value: does the operator/test care about the static action_table + the redirect tap, or only the per-slot match maps]
- **`resolve_ifindex` offline handling:** stub-resolver-in-harness + symbolic ifindex (recommended, all four lenses) vs thread a resolved index through the render path as a parameter (cleaner signature, but touches prod call sites). [genuine-PO-value: keep prod signatures frozen vs cleaner parameterization]
- **Does the live gate need to actually shrink (Option 4 slice 2 / test-boundary B), or is additive offline coverage (Option 1 / test-boundary A) sufficient this round?** [genuine-PO-value: B39-extension gate-shrink goal vs new-coverage-only]
- **Is a typed `MapImage` API (Option 3) wanted, or is the link-seam's SSoT-by-construction sufficient (no consumer for the typed structure exists)?** [genuine-PO-value: speculative API generality vs YAGNI]

## Hidden assumptions

- **The fake `xdpfilter_bpf` skel + fake `bpf_map__fd` can be linked libbpf-free against the real skeleton struct type.** If the generated skel header transitively forces libbpf symbols un-satisfiable by fakes, the clean-link contract breaks and the recommendation shifts toward Option 3. [needs-spike]
- **The `materialize` + render-helper + `populate_action_table` + `populate_redirect_devmap` extraction into `materialize.cpp` is a PURE move with zero logic change** (mirroring B40/B34b), leaving insn 3477 / PASS-DROP byte-identity / PI-mvp-4.35-VERDICT-IDENTITY structurally untouched. If any helper has a hidden dependency on the rest of loader.cpp's TU (statics, anon-namespace, file-local helpers), the "pure move" becomes a real refactor and Option 1's low-risk rating weakens. [needs-grep]
- **The bulk-clear (`get_next_key`→delete) traffic is genuinely NOT part of the config→map-image truth** and excluding it from the golden loses no correctness coverage. If a test class depends on clear-ordering, the "occupied-writes-only" image under-asserts. [needs-grep]
- **The named SPLIT ctests (`T_APPLY_VALID_CONFIG`, `T_APPLY_REPLACES_RULESET`, `T_REDIRECT_COUNTER_AND_MAP`) cleanly separate an image-side assertion from a verdict-side assertion.** If their `bpftool map dump` and frame-inject blocks are entangled, Option 4 slice 2's "delete the image half" becomes risky surgery rather than a clean cut. [needs-grep]
- **`materialize`'s in-image writes read nothing from the live skel beyond map fds** (i.e. the rendered image is a pure function of `CompiledRuleset` + `slot` + resolved-ifindex). Verified the live READ leakage is confined to `copy_rule_counters_forward` (loader.cpp:2165-2293), which reads the OLD-active inner and is called OUTSIDE the apply write-set — so it is correctly EXCLUDED from the image. If any in-image write secretly depended on a live read, the offline render would be incomplete; the code shows it does not. [needs-grep]
- **(NEW — resolves the action_table/devmap hook) The offline image's correct scope is the FULL apply write-set, and the two writes outside `materialize()` (`populate_action_table` at loader.cpp:2146/2266, `populate_redirect_devmap` at 2154/2273) must be issued by the harness in the apply-site order to reproduce image-format's recommended golden.** Verified: `materialize` (1637-1713) does NOT issue them; the recommended golden contains `action_table` + `redirect_devmap` rows. If instead the PO/design rules the image is `materialize()`-body-only, the golden format must DROP those two rows and the round's claim "the redirect tap moves offline" weakens. The recommendation assumes full-apply-write-set scope. [grounded for the call-site facts; needs-grep for the extraction packaging]

---

# Discharge ledger (grounder — Phase 5.5)

**Verdict: `clean-with-gates`** — 0 refuted, 1 required spike (SPIKE-1), 3 surviving needs-grep (slice-time rechecks for the briefer), 4 disguised-PO reclassified off the human plate. The heavily-cited core is real; risk is concentrated in the spike, not a wrong plan.

# Discharge ledger — `apply --dry-run` output contract + offline/live test boundary (grounder, round 2)

## Verdict
**`clean-with-gates`** — NO sizing or sequencing claim is FALSE against the code; the synthesis's verified core is genuinely solid. But the verdict CANNOT be `clean`: the recommended Option 1's entire "low-risk slice 1" rating rests on **one undischarged load-bearing spike** (fake-skel libbpf-free link) plus **2 surviving needs-grep** rechecks. Ground truth shows the spike is NOT statically clean — `build/xdpfilter.skel.h` `#include <bpf/libbpf.h>`. A green "clean" word over that is precisely the 2026-05-30 failure class this phase exists to kill. Separately: this is a near-`po-leak` — **4 of the 6 synthesis "open questions (need human input)" were disguised engineering** and are pulled off the human's plate below (kept at clean-with-gates only because the 2 genuine PO forks DO survive the filter and no sizing claim was refuted).

## Discharge table
| # | Synthesis claim (verbatim / tight paraphrase + where) | Class | Tag | Evidence / action |
|---|---|---|---|---|
| 1 | "The seam is uniform — render helpers already take `int fd`; the ONLY skel weld is inside `materialize()`'s body (~12 `skel->maps.*` derefs)." | sizing | **grounded** | VERIFIED. `populate_hash_inner_slot(int inner_fd,…)` loader.cpp:1182; `populate_bitvec_inner_slot(int inner_fd,…)`:1235; `populate_port_inner_slot(int inner_fd,…)`:1294; `populate_rules_inner_slot(int rules_inner_fd,…)`:1380; `write_ruleset_state(int…fd,…)`:1331; `write_slot_rule_id(int…fd,…)`:1425. None reference `skel`. `materialize` (1637-1713) is the sole site of `inactive_axis_fd(skel->maps.X_a, X_b,…)` (9 axes) + 2 direct `bpf_map__fd(skel->maps.ruleset_state/slot_rule_id)` (1688/1707). Claim correct. |
| 2 | "The LPM mask is the post-closure output of `close_fn(prefixes)` computed INSIDE `populate_bitvec_inner_slot`, absent from `CompiledRuleset` ⇒ kills the CompiledRuleset-print oracle." | hidden-assumption / sizing | **grounded** | VERIFIED at loader.cpp:1266 `const std::vector<std::uint64_t> closed = close_fn(prefixes);`. `close_prefixes`/`close_prefixes6` are external-linkage (compiled_ruleset.hpp:126-130) returning `vector<uint64_t>`, called BY name from materialize, applied at materialize-time not compile-time. Oracle-scope argument holds. **Staleness note:** this is a volatile code fact → sliceTimeRecheck. The 2026-05-30 32-bit-trap does NOT recur (closure is over the 64-rule SLOT bitmask, not the 128-bit address; v6 closure already exists). |
| 3 | "`action_table` + `redirect_devmap` are issued OUTSIDE `materialize()`, at the apply call sites; the offline image must be the FULL apply write-set." (HA #6 / WRITE-SET BOUNDARY) | sizing / sequencing | **grounded** | VERIFIED. `populate_action_table` called at 2146 & 2266; `populate_redirect_devmap` at 2154 & 2273 — both OUTSIDE `materialize` (1637-1713). `materialize` issues only the 9 axes + ruleset_state + rules + slot_rule_id. Harness MUST replicate the 3-call apply sequence to produce image-format's recommended golden. Correct, and the reviewer's UNADDRESSED-HOOK is properly resolved. → sliceTimeRecheck (call order is volatile). |
| 4 | "`copy_rule_counters_forward` is correctly EXCLUDED from the image (reads old-active inner = irreducibly live)." (HA #5) | hidden-assumption | **grounded** | VERIFIED. `copy_rule_counters_forward(int old_active_inner_fd, int inactive_inner_fd,…)` loader.cpp:1496, called at 2188/2293, READS the old-active inner. Correctly outside the config→image truth. |
| 5 | "`parse_apply` is at `src/cli/cli.cpp:226` (corrects the prior :369 mis-citation; 369/370 is only `return parse_apply(rest)`)." | sizing | **grounded** | VERIFIED. `parse_apply` defined cli.cpp:226; :370 is the dispatch. The synthesis self-corrected an earlier :369 error — good. → sliceTimeRecheck (CLI line numbers are volatile; this fact already aged once). |
| 6 | "No `dry-run`/`dry_run`/`dryrun` token exists anywhere in `src/` — net-new surface." | sizing | **grounded** | VERIFIED. `grep -rn 'dry.run\|dry_run\|dryrun' src/` = NONE FOUND. → sliceTimeRecheck. |
| 7 | "The offline harness follows the `compile_harness`/`ruleset_delta_harness` mold — bare-main, libbpf-free link IS the contract, no `require_passwordless_sudo`/`XDPMF_CI_BUILD_ONLY`/`RESOURCE_LOCK`." | sizing | **grounded** | VERIFIED. Both harnesses exist (tests/compile/compile_harness.cpp, tests/delta/ruleset_delta_harness.cpp); CMake blocks tests/CMakeLists.txt:1637 & 1672 carry the literal `# DELIBERATELY no … PkgConfig::LIBBPF` contract (1651/1685). Mold is real and copyable. |
| 8 | "The named SPLIT ctests (`T_APPLY_VALID_CONFIG`, `T_APPLY_REPLACES_RULESET`, `T_REDIRECT_COUNTER_AND_MAP`) cleanly separate image-side from verdict-side." (HA #4 / Option 4) | sizing / sequencing | **needs-grep (partially discharged → survives)** | PARTIALLY VERIFIED for `T_APPLY_VALID_CONFIG`: image-side `bpftool map dump` assertions at lines 91-144, verdict-side frame-inject at 150+ — genuinely split. `T_REDIRECT_COUNTER_AND_MAP` contains both `redirect_devmap`/`bpftool` and `STAT_REDIRECT` — consistent with split. **NOT exhaustively confirmed for `T_APPLY_REPLACES_RULESET` line-by-line.** This gates Option 4 slice-2 only (not the recommended slice 1) → survives as a slice-time recheck for IF/WHEN Option 4 slice-2 is scoped. |
| 9 | "TTFW ~1 cycle (Option 1); LOC ~0 net prod (pure move) + N test TUs." | sizing | **needs-spike-blocked** | The TTFW/LOC estimate is downstream of claim #11 (pure-move) AND the spike (#10). If the fake-skel link FAILS libbpf-free, the mechanism changes and TTFW grows. Cannot promote to grounded while the spike is open. Stays gated. |
| 10 | "The fake `xdpfilter_bpf` skel + fake `bpf_map__fd` can be linked libbpf-free against the real skeleton struct type." (HA #1 / Option-1 Open Q / the synthesis's 'single biggest caveat') | hidden-assumption | **needs-spike** | NOT statically clean. `build/xdpfilter.skel.h:9 #include <bpf/libbpf.h>`; the `xdpfilter_bpf` struct + `static inline` fns call `bpf_object__*` (line 166+) and embed a multi-thousand-line `__elf_bytes`. Using `sizeof(xdpfilter_bpf)`/`skel->maps.X` requires that header → drags libbpf decls. `bpf_map__fd` is a libbpf symbol (not defined in repo → fakeable at link). Whether the libbpf-calling inlines are *required* at link is a real compile question. → **required pre-slice spike** (see spikes[]). This is THE gate. |
| 11 | "The `materialize` + render-helpers + `populate_action_table` + `populate_redirect_devmap` extraction into `materialize.cpp` is a PURE move with zero logic change (mirroring B40/B34b)." (HA #2 / Option-1 slice step 1) | hidden-assumption / sizing | **needs-grep (wrinkle surfaced)** | PARTIALLY REFUTED-IN-SPIRIT but not a sizing refutation: the helpers live inside the **anonymous namespace** (opens loader.cpp:78) and call file-local `throw_loader`/`classify` (anon-ns, loader.cpp:341/350) and `inactive_axis_fd` (1610). A clean extraction must ALSO move or share these (e.g. promote `throw_loader` to a header). So "pure move, zero logic change" is mechanically LARGER than implied — still low-risk and logic-preserving, but not a literal cut-paste of one function. Briefer must scope the dependency cluster. → sliceTimeRecheck folded into the spike's slice. |
| 12 | "The bulk-clear (`get_next_key`→delete) traffic is genuinely NOT part of the config→map-image truth; excluding it loses no coverage." (HA #3) | hidden-assumption | **grounded (architectural)** | The clear traffic is kernel-state-dependent (you can only delete keys that exist in the live map) — by construction non-deterministic offline and not a function of `Config`. Excluding it is the correct offline/live boundary. Architectural invariant, not volatile. |
| 13 | "Option 3 (factored `render()→MapImage`) rewrites the live write path ⇒ re-exposes PI-mvp-4.35-VERDICT-IDENTITY / atomic-swap ordering / insn 3477; HIGHER risk than Option 1's structural SSoT." | sequencing / sizing | **grounded** | Consistent with code: Option 1's link-seam runs the *identical* `materialize` object code (claim #1) ⇒ no live-write-path change ⇒ byte-identity structural. Option 3 rewrites the write loop ⇒ must re-prove byte/order identity via the live gate. The relative-risk ordering (Opt1 < Opt3) is sound. |
| 14 | "Human-vs-golden-vs-both for slice 1 → PO." | open-Q | **genuine-PO-value (SURVIVES)** | PO filter PASSES: the external value is operator-facing coherence + product scoping (does a GGSN-Gi NOC operator get the decoded preview THIS cycle). Engineering default is golden-only-now (no rework to add human later, shared image), but whether the named roadmap UX must land now is a product-priority call. Kept on plate. |
| 15 | "Does the live gate need to actually shrink (Opt4 slice-2 / test-boundary B), or is additive coverage (Opt1) sufficient?" | open-Q | **genuine-PO-value (SURVIVES)** | PO filter PASSES: external value is risk appetite + B39-extension goal priority. The live-.sh thinning trades a real surgery risk (could delete live verdict coverage) for gate-shrink; whether shrinking is worth it this round is product/risk, not code. Kept on plate. |
| 16 | "Is the `apply --dry-run` CLI verb in scope this round, or a thin follow-on?" | open-Q | **genuine-PO-value → grounded (RECLASSIFIED OFF PO)** | PO filter FAILS standalone. In-process harness (compile_harness precedent) needs NO CLI verb for the test win — pure code fact. The only PO-flavored residue ("does the operator want the verb shipped now") is identical to item #14, already on the plate. Removed as a duplicate engineering question. |
| 17 | "Does the offline image cover the full apply write-set or `materialize()`-body only?" | open-Q | **genuine-PO-value → needs-grep (RECLASSIFIED OFF PO)** | PO filter FAILS. Pure code fact, fully discharged in claim #3: the golden contains action_table+devmap rows, those writes are outside materialize ⇒ scope MUST be full apply write-set or the golden is unproducible. Decided by grep, not by "does the operator care". The synthesis tagged its own facts `grounded` then routed the ratification to PO — that is the leak. Removed. |
| 18 | "`resolve_ifindex` offline: stub-resolver vs thread-resolved-index-through-render → PO." | open-Q | **genuine-PO-value → needs-grep (RECLASSIFIED OFF PO)** | PO filter FAILS. Governed by code invariants: 'zero kernel calls' forbids resolve_ifindex (loader.cpp:1583→if_nametoindex) in dry-run; 'keep prod signatures frozen' favors the harness-side stub. All four architects converged. Invariant-driven, not product/priority. Removed. |
| 19 | "Is a typed `MapImage` API (Opt3) wanted, or is link-seam SSoT sufficient?" | open-Q | **genuine-PO-value → needs-spike (RECLASSIFIED OFF PO)** | PO filter FAILS. YAGNI/architecture call: no consumer for typed MapImage exists (contrarian-verified); link-seam delivers SSoT without the verdict-identity-risking rewrite. 'Speculative generality vs YAGNI' with no named consumer is engineering. Removed. |

## Refuted claims
**None.** No sizing or sequencing claim is false against the code. The synthesis's grounded facts (claims #1–#7, #12–#13) all verify against loader.cpp / cli.cpp / the skel header / the ctest scripts. This is the synthesis paying for its round-2 coherence-review: the heavily-cited core is real. The risk is entirely concentrated in the **undischarged spike (#10)** and one **size-wrinkle (#11)**, not in a wrong plan.

## PO plate (after filtering)
Two questions survive the filter — both carry a NAMED external value a competent engineer with full code access still could not resolve:
1. **Slice-1 representation: golden-only vs co-ship the human-decoded operator preview.** External value: operator-facing coherence + product/TTFW scoping of the named roadmap UX. (Engineering lean: golden-only now; human view is a zero-rework follow-on.)
2. **Must the live datapath gate actually SHRINK this round (Opt4 slice-2 thinning), or is additive offline coverage enough?** External value: risk appetite + the B39-extension goal's priority (gate-shrink trades a live-.sh surgery risk for the shrink).

## Reclassified-off-PO (anti-leak record)
The synthesis routed SIX items to "open questions (need human input)". FOUR were disguised engineering and are pulled back:
- **"CLI verb in scope this round?"** → in-process harness needs no verb (compile_harness precedent); residue is a dup of PO item #1. → **grounded**.
- **"Full apply write-set vs materialize()-body-only?"** → pure code fact, discharged (action_table/devmap at 2146/2154/2266/2273, outside materialize). → **needs-grep**.
- **"resolve_ifindex stub vs thread-through?"** → invariant-driven ('zero kernel calls' + 'freeze prod signatures'), all four architects converged. → **needs-grep**.
- **"Typed MapImage API wanted?"** → YAGNI, no consumer, link-seam delivers SSoT without rewrite. → **needs-spike**.
This is the audit trail that "defer to PO" was not used as an escape hatch for 4 engineering decisions.

## Required pre-slice spikes (needs-spike)
**SPIKE-1 (gates Option 1 slice 1 — THE gate):** Prove a test TU including the generated `xdpfilter.skel.h` (for the `xdpfilter_bpf` type), using a fake `xdpfilter_bpf*` (`sizeof` + `skel->maps.X` derefs) and providing fake `bpf_map__fd` / `bpf_map_update_elem` / `bpf_map_get_next_key` / `bpf_map_delete_elem` / `resolve_ifindex` symbols, **links with NO real libbpf**.
- **Why it's a spike not a grep:** `build/xdpfilter.skel.h:9 #include <bpf/libbpf.h>`; the type drags libbpf decls and the header carries static-inline libbpf-calling fns + an embedded ELF byte array. Whether those get required at link is a *compile/link* fact, undecidable from source reading.
- **PASS:** `dryrun_harness` target links + runs with no `PkgConfig::LIBBPF` / no `*_skel` dep (matches tests/CMakeLists.txt:1651/1685). **FAIL:** link pulls `bpf_object__*` etc. → degrade: either Option 3's factored `render()` (sidesteps the skel via `CompiledRuleset`) or a hand-rolled minimal fake-skel struct that does NOT `#include xdpfilter.skel.h`. A FAIL re-scopes slice 1; it does not kill the round.

## Slice-time rechecks (needs-grep that survived)
1. **parse_apply @ cli.cpp:226 + no dry-run token in src/** — CLI line numbers are volatile and this fact already aged once (:369→:226 self-correction). Re-run before threading the flag.
2. **The 3-call apply write-set sequence** `materialize → populate_action_table → populate_redirect_devmap` (currently 2136/2146/2154 and 2258/2266/2273), with `copy_rule_counters_forward` (2188/2293) staying OUTSIDE — re-confirm before fixing the golden's map-order; any apply-path refactor reorders these.
3. **`close_prefixes`/`close_prefixes6` still return `vector<uint64_t>` and closure still at loader.cpp:1266 inside `populate_bitvec_inner_slot`** — the fact that kills the CompiledRuleset-print oracle. (Pinned because a once-true sizing fact, the 2026-05-30 close_prefixes 32-bit type, is exactly this kind of fact that ages.)
4. **(Option 4 slice-2 only) Per-file image/verdict split of `T_APPLY_REPLACES_RULESET`** — only `T_APPLY_VALID_CONFIG` (lines 91-144 image / 150+ verdict) and `T_REDIRECT_COUNTER_AND_MAP` were confirmed split; `T_APPLY_REPLACES_RULESET` not line-verified. Re-grep before any "delete the image half" surgery.
5. **(folded into SPIKE-1's slice) The `materialize.cpp` extraction is NOT a literal pure-move** — helpers are in the anon-namespace (loader.cpp:78) and call file-local `throw_loader`/`classify` (341/350); the extraction must move/share that cluster. Scope the dependency set before estimating LOC/TTFW.