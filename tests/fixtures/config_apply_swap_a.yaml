# Apply-A ruleset for §6.23 + §6.24. §5.43 MVP-4.3: MAC→src_cidr (deferred).
# Pass src 10.0.0.0/8 only.
schema_version: 2
default_action: drop
rules:
  - id: 0
    action: pass
    match:
      src_cidr: "10.0.0.0/8"
