# §6.22 sub-case 5: forward-compat hinge for MVP-3.2.
# Per design row: `match: {cidr: 10.0.0.0/8}` — flow-form AND unsupported
# match type. EITHER rejection path (parser-side flow-form OR
# validator-side unsupported-match) → exit 9; the test allows both.
default_action: drop
rules:
  - id: 0
    action: pass
    match: {cidr: 10.0.0.0/8}
